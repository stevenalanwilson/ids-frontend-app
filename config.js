// import dotenv from 'dotenv'
// dotenv.config()

module.exports = {
  appVersion: '0.1.0',
  appName: 'Ideal Design Soloutions frontend application',
  environment: process.env.NODE_ENV,
  space: process.env.SPACEID,
  accessToken: process.env.ACCESSTOKEN
}
